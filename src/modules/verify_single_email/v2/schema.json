{
  "metadata": {
    "workflows_module_schema_version": "1.0.0"
  },
  "fields": [
    {
      "id": "verification_id",
      "type": "string",
      "label": "Verification ID",
      "description": "The verification ID returned from the initial email verification request",
      "validation": {
        "required": true
      },
      "ui_options": {
        "ui_widget": "input",
        "placeholder": "abc123-def456-ghi789"
      }
    },
    {
      "type": "connection",
      "id": "api_connection",
      "label": "BounceBan API Key",
      "description": "Select your connected BounceBan API key. You can find your API key at https://bounceban.com/app/api/settings",
      "allowed_app_types": ["hyperline"],
      "allowed_connection_management_types": ["managed", "custom"]
    }
  ],
  "ui_options": {
    "ui_order": ["verification_id", "api_connection"]
  }
}